# Cron 部署配置（每台新机器复制为 .env 并填写）
#
# 这些变量在 cron_export.py 导出模板时被替换成 ${VAR}；
# 在 cron_deploy.py 部署时，再用此处的真实值代入。

# 微信用户 openid（接收消息的人）
WECHAT_USER_ID=o9cq80z60LaB2jyf8JO9xNWsynN4@im.wechat

# 微信 bot 账号 ID（哪个机器人发消息）
WECHAT_BOT_ACCOUNT_ID=75c2b3e86437-im-bot

# 工作区根目录（脚本/报告/规则都在这里）
WORKSPACE_ROOT=/root/.openclaw/workspace
